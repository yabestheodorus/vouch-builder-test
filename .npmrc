# Skip pnpm's pre-script auto-install check. It mis-detects the workspace root
# when a build script (e.g. next build) is run from inside a workspace package,
# triggering a spurious install. Dependencies are managed by explicit installs.
verify-deps-before-run=false
